// src/sandbox/runtime.ts
// Sandbox React runtime — minimal React 19 mount inside the sandboxed iframe.
// Receives compiled component code via postMessage and dynamically renders it.
//
// Full implementation in T037. This placeholder establishes the module
// for TypeScript compilation.

/** Placeholder — full sandbox runtime implementation in T037. */
export function initRuntime(): void {
  // TODO: T037 — mount React root, handle RENDER_DATA messages,
  // execute compiled component bundle via dynamic evaluation
}
