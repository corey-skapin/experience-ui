// src/shared/types/index.ts
// Cross-process TypeScript type definitions.
// All types shared between main process, renderer, and sandbox are defined here.
//
// Full type definitions (APISpec, NormalizedSpec, InterfaceVersion, etc.)
// will be implemented in T007 based on data-model.md.
//
// This placeholder file ensures TypeScript project references compile
// without errors during Phase 1 setup.

/** Placeholder — full type definitions in T007. */
export type Placeholder = Record<string, never>;
