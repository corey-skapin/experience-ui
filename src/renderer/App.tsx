/**
 * Top-level App component.
 * Provides the root layout shell — the split-pane container will be implemented in Phase 3.
 * For Phase 1, this is a minimal placeholder to verify the renderer pipeline works.
 */
import type { ReactElement } from 'react'

export default function App(): ReactElement {
  return (
    <div className="app-root">
      <p>Experience UI — initializing…</p>
    </div>
  )
}
